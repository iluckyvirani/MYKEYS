══════════════════════════════════════════════════════════════════════════════
                    MYKEYS — FEATURE PHASES PLANNING DOCUMENT
              Packages · Property Docs · Bidding · Chat Inquiries
══════════════════════════════════════════════════════════════════════════════

Last Updated : 8 May 2026
Tech Stack   : Next.js 15 (App Router) · TypeScript · Prisma ORM · PostgreSQL
               Tailwind CSS · Razorpay · Cloudinary
Currency     : GBP (£)
Auth         : JWT (access + refresh tokens)

CODEBASE CONVENTIONS
────────────────────
  Folder naming: all src/components/* subfolders must be lowercase
    (buy/, rent/, hero/, search/, layout/, etc.)
    Component file names remain PascalCase (BuyHero.tsx, LongRentHero.tsx)

  service/ vs services/ (INTENTIONAL distinction — do not change):
    app/services/        → public consumer pages (browse, register)
    app/service/         → service provider dashboard
    api/services/        → public API (book, categories, providers)
    api/service/         → provider API (earnings, profile, requests)
    components/services/ → shared service UI components

  types/ naming:
    One canonical file per domain. Use bookings.ts (plural) not booking.ts.
    If unsure, use plural for collection types (bookings, payments, packages).

  components/admin/ will be created at the start of Phase 1 when first
    admin-specific component is added.

TABLE OF CONTENTS
─────────────────
  PHASE 1 — Owner Packages (Mandatory to Publish Properties)
  PHASE 2 — Property Documents (Admin-Defined Required Documents)
  PHASE 3 — Bidding / Boost for Short Rent Properties
  PHASE 4 — Chat-Based Inquiries with Reminders
  DEPENDENCY MAP
  DATABASE CHANGES SUMMARY
  API ENDPOINTS SUMMARY

══════════════════════════════════════════════════════════════════════════════
  WHY THIS ORDER?  — DEPENDENCY LOGIC
══════════════════════════════════════════════════════════════════════════════

  Phase 1 FIRST  → Packages gate LONG_RENT and BUY publishing.
                   Also introduces AdminSettings model (used by Phase 3 commission
                   and bid config). SHORT_TERM publishes freely; commission % set here.

  Phase 2 SECOND → Document requirements apply to LONG_RENT and BUY properties.
                   Comes after Phase 1 since docs are relevant to gated listings.
                   SHORT_TERM doc types can optionally be configured too.

  Phase 3 THIRD  → Bidding is SHORT_TERM only. No package dependency.
                   Reuses AdminSettings from Phase 1 for bid config.
                   Can be built in parallel with Phase 2 by a second developer.

  Phase 4 LAST   → Chat inquiries sit on top of the full property & package system.
                   Reminders reference existing users, properties, and packages.
                   Can be built while Phase 3 is being tested (parallel dev possible).

══════════════════════════════════════════════════════════════════════════════
  PHASE 1 — OWNER PACKAGES
  "Packages required only for LONG RENT and BUY listings"
══════════════════════════════════════════════════════════════════════════════

─────────────────────────────────────────
  1.1  OVERVIEW
─────────────────────────────────────────

  Today the Package and OwnerPackage models exist in the schema but are NOT
  enforced when an owner tries to publish a property. Phase 1 makes package
  purchase mandatory for LONG RENT and BUY properties only.

  SHORT RENT (SHORT_TERM) properties are completely free to publish —
  no package is required. Short rent has its own separate monetisation
  via the admin commission system (see section 1.10) and bidding (Phase 3).

  Summary by listing type:
  ┌─────────────────────────┬───────────────────────────────────────────────┐
  │ Listing Type            │ Package needed to go live?                   │
  ├─────────────────────────┼───────────────────────────────────────────────┤
  │ LONG_RENT               │ YES — must have active package                │
  │ BUY                     │ YES — must have active package                │
  │ SHORT_TERM (short rent) │ NO  — free to publish; admin takes commission  │
  └─────────────────────────┴───────────────────────────────────────────────┘

  Packages are FULLY DYNAMIC — there are no fixed tiers (no BASIC/STANDARD/
  PREMIUM). The Admin creates any number of packages with any name and any
  combination of features. Examples of what admin might create:

  ┌─────────────────────┬──────────────────────────────────────────────────┐
  │ Package Name        │ Admin decides all fields freely                  │
  │ (Admin chooses)     │                                                  │
  ├─────────────────────┼──────────────────────────────────────────────────┤
  │ e.g. "Starter"      │ Admin sets: price, duration, property limit,     │
  │ e.g. "Pro"          │ feature flags, featured slots — completely free  │
  │ e.g. "10-Day Trial" │ choice. No predefined defaults enforced.         │
  │ e.g. "Annual Plus"  │                                                  │
  └─────────────────────┴──────────────────────────────────────────────────┘

─────────────────────────────────────────
  1.2  PACKAGE FEATURES (FULLY CONFIGURABLE)
─────────────────────────────────────────

  Every package has the following fields. Admin sets EACH ONE freely when
  creating or editing a package — there are no presets or locked defaults.

  Field                            │ Type    │ What it controls
  ─────────────────────────────────┼─────────┼──────────────────────────────────
  Package Name                     │ Text    │ Display name shown to owners
  Price                            │ Number  │ Cost in GBP
  Duration Value                   │ Number  │ e.g. 10, 1, 6
  Duration Unit                    │ Select  │ days / months / years
  Property Limit                   │ Number  │ Max live listings (0 = unlimited)
  Featured Slots                   │ Number  │ How many can be featured
  Show Owner Name on listing       │ Yes/No  │ Owner full name visible publicly
  Show Owner Phone on listing      │ Yes/No  │ Owner phone visible publicly
  Inquiries go directly to owner   │ Yes/No  │ Inquiry emails/chat to owner
  Admin CC'd on all inquiries      │ Yes/No  │ Admin also receives every inquiry
  Full admin support               │ Yes/No  │ Dedicated admin handling
  Property document expiry alert   │ Yes/No  │ Notifications when docs near expiry
  Short Description                │ Text    │ Shown on the package card
  Is Active                        │ Yes/No  │ Visible to owners for purchase

─────────────────────────────────────────
  1.3  ADMIN — PACKAGE MANAGEMENT
─────────────────────────────────────────

  Where: Admin Dashboard → "Packages" section (already partially exists)

  NOTE: These packages apply to LONG_RENT and BUY properties ONLY.
        SHORT_TERM properties are NOT affected by packages.

  Actions Admin can do:
  • Create a new package  → set name, price, duration (number + unit),
                            all feature flags, property limit, featured slots
  • Edit existing package → change any field; existing subscriptions are
                            NOT affected until their renewal date
  • Deactivate package    → new owners cannot buy it; existing owners unaffected
  • Delete package        → only if no active OwnerPackage subscriptions exist

  Required Fields when creating a package (all decided by Admin freely):
  ─────────────────────────────────────────────────────────────────────
  - Package Name           (text, required — Admin chooses any name)
  - Price                  (number, GBP)
  - Duration Value         (number, e.g. 10 / 1 / 6)
  - Duration Unit          (days / months / years)
    Examples: 10 days  → expires 10 days after purchase
              1 month  → expires 1 month after purchase
              1 year   → expires 1 year after purchase
  - Property Limit         (number; enter 0 for unlimited)
  - Featured Slots         (number)
  - Show Owner Name        (toggle)
  - Show Owner Phone       (toggle)
  - Direct Inquiry to Owner(toggle)
  - Admin CC on Inquiry    (toggle)
  - Full Admin Support     (toggle)
  - Doc Expiry Alert       (toggle)
  - Short Description      (optional text)
  - Is Active              (toggle — controls visibility to owners)

─────────────────────────────────────────
  1.4  OWNER — BUYING A PACKAGE
─────────────────────────────────────────

  Flow:
  1. Owner registers / logs in as OWNER role
  2. Owner can add and save ALL property types at any time — no package needed
     to create or edit. Properties are saved as DRAFT.
  3. SHORT_TERM properties: owner can publish (set ACTIVE) immediately,
     no package required. Admin commission is applied on each booking.
  4. LONG_RENT / BUY properties: owner must buy a package to go ACTIVE.
  5. Owner goes to Owner Dashboard → "My Package" tab
  6. Sees current package status (if any) or "No active package"
  7. Clicks "Browse Packages" — sees all active packages with their
     duration clearly shown (e.g., "10 Days", "1 Month", "1 Year")
  8. Selects a package → Razorpay checkout opens
  9. Payment success → OwnerPackage record created with:
       status    = ACTIVE
       startDate = today (purchase date)
       endDate   = calculated from package duration:
                     days   → startDate + durationValue days
                     months → startDate + durationValue months
                     years  → startDate + durationValue years
       Example: Admin set duration = 10 days
                Owner buys on 8 May → endDate = 18 May
  10. Owner's DRAFT LONG_RENT / BUY properties can now be published
      up to the package property limit

  What happens if package expires:
  • All owner's ACTIVE LONG_RENT / BUY properties become INACTIVE (cron job)
  • SHORT_TERM properties are NOT affected — they stay ACTIVE
  • Properties are NOT deleted — owner keeps them as INACTIVE drafts
  • Owner sees a banner: "Your package expired — renew to re-activate listings"
  • Owner can renew (same package) or upgrade → properties go ACTIVE again

  What happens before owner buys a package:
  • Owner CAN add, edit, save ALL property types freely (status = DRAFT)
  • SHORT_TERM: owner can publish immediately — no package required
  • LONG_RENT / BUY: NOT shown publicly until owner buys a package
  • Owner Dashboard shows banner for LONG_RENT/BUY drafts: "Buy a package to
    make your Long Rent / Buy listings live"
  • A "Buy Package" button shown next to DRAFT LONG_RENT / BUY properties only

─────────────────────────────────────────
  1.5  PROPERTY PUBLISHING GATE
─────────────────────────────────────────

  Key principles:
  • Owner can CREATE and SAVE all property types at any time (no package needed)
  • SHORT_TERM: owner can set status → ACTIVE freely, no package check
  • LONG_RENT / BUY: owner can only set status → ACTIVE if they have a
    valid package with remaining property slots

  Backend enforcement (on PATCH status → ACTIVE):
  • If property.listingType = LONG_RENT or BUY:
      1. Check owner has active OwnerPackage (status=ACTIVE, endDate > now)
      2. Check propertiesUsed < package.propertyLimit
      3. If either fails → return 403 with clear reason
  • If property.listingType = RENT and rentalType = SHORT_TERM:
      → No package check. Allow publish immediately.

  Creating / editing a property (no package check — always allowed):
  • POST /api/properties → allowed for any owner, status defaults to DRAFT
  • PATCH /api/properties/[id] (edit fields) → always allowed
  • PATCH /api/properties/[id] (status → ACTIVE) → package check only for
    LONG_RENT and BUY

  Public property listing / search queries:
  • Only return properties where status = ACTIVE
  • DRAFT / INACTIVE properties are never shown publicly

  Frontend enforcement:
  • Property form: save as draft works always
  • SHORT_TERM: "Publish" button always enabled
  • LONG_RENT / BUY: "Publish" button disabled with tooltip if no active package
  • Property list in owner dashboard shows:
      - DRAFT badge + "Buy Package to Publish" for LONG_RENT/BUY without package
      - "3 of 5 properties used" usage counter (LONG_RENT/BUY) when package active
  • Package banner shown only for owners with LONG_RENT / BUY drafts

─────────────────────────────────────────
  1.6  PROPERTY DETAIL PAGE — OWNER CONTACT VISIBILITY
─────────────────────────────────────────

  On the public property detail page (/property/[id]):

  The contact section is driven entirely by the feature flags of whatever
  package the owner currently has active — not by any fixed tier name.

  Flag on owner's package             │ What user sees
  ────────────────────────────────────┼──────────────────────────────────────
  showOwnerName = false               │ Owner name hidden
  showOwnerName = true                │ Owner full name shown
  showOwnerPhone = false              │ Phone hidden
  showOwnerPhone = true               │ Owner phone number shown
  directInquiryToOwner = false        │ Inquiry goes to Admin only
  directInquiryToOwner = true         │ Inquiry goes directly to owner
  adminCCOnInquiry = true             │ Admin also receives a copy

  These flags combine — e.g. an owner with showOwnerName=true,
  showOwnerPhone=false, directInquiryToOwner=true gets: name visible,
  no phone, inquiry direct to owner.

─────────────────────────────────────────
  1.7  DATABASE CHANGES — PHASE 1
─────────────────────────────────────────

  Existing Package model — changes:
  ──────────────────────────────────────────────────
  // 1. Remove:  tier      PackageTier  (no longer needed — no fixed tiers)
  // 2. Replace: duration  String       (was "monthly"/"yearly")
  // With:
  durationValue          Int      @default(1)       // e.g. 10
  durationUnit           String   @default("months") // "days" | "months" | "years"

  // 3. Add feature flag fields (all default false — admin opts each in):
  showOwnerName          Boolean  @default(false)
  showOwnerPhone         Boolean  @default(false)
  directInquiryToOwner   Boolean  @default(false)
  adminCCOnInquiry       Boolean  @default(false)
  fullAdminSupport       Boolean  @default(false)
  docExpiryAlert         Boolean  @default(false)
  bidFeatureEnabled      Boolean  @default(false)
  // NOTE: bidFeatureEnabled removed — bidding is open to all owners
  //       with SHORT_TERM properties, no package required

  // 4. Keep unchanged: name, price, propertyLimit, featuredLimit,
  //    storageLimit, features Json, isActive, description
  // NOTE: The PackageTier enum in schema.prisma can also be removed
  //       once the tier column is dropped.

  endDate calculation helper (used when creating OwnerPackage):
    if durationUnit = "days"   → add durationValue days to startDate
    if durationUnit = "months" → add durationValue months to startDate
    if durationUnit = "years"  → add durationValue years to startDate

  No new models needed for Phase 1.
  New migration: 20260508_add_package_feature_flags

─────────────────────────────────────────
  1.8  NEW / UPDATED API ENDPOINTS — PHASE 1
─────────────────────────────────────────

  POST   /api/admin/packages              Create package (admin only)
  GET    /api/admin/packages              List all packages (admin only)
  PATCH  /api/admin/packages/[id]         Edit package (admin only)
  DELETE /api/admin/packages/[id]         Delete package (admin only)
  GET    /api/packages                    Public list of active packages
  GET    /api/packages/[id]               Public single package detail
  POST   /api/owner/packages/subscribe    Owner buys a package (Razorpay)
  GET    /api/owner/packages/current      Owner's active package info
  POST   /api/owner/packages/renew        Renew/upgrade package

  Updated:
  POST   /api/properties                  Saves as DRAFT always
  PATCH  /api/properties/[id]             Status → ACTIVE: checks package only
                                          for LONG_RENT / BUY; SHORT_TERM free
  GET    /api/properties/[id]             Returns owner contact based on package

  Admin Settings (commission):
  GET    /api/admin/settings              Get all admin settings
  PATCH  /api/admin/settings              Update settings (incl. short rent
                                          commission %)

─────────────────────────────────────────
  1.9  FRONTEND PAGES / COMPONENTS — PHASE 1
─────────────────────────────────────────

  Admin:
    /admin/packages                       List all packages
    /admin/packages/new                   Create package form
    /admin/packages/[id]/edit             Edit package form

  Owner Dashboard:
    /owner/dashboard → "My Package" tab   Shows current package, expiry,
                                          usage stats, renew/upgrade button
    /owner/packages                       Browse & buy packages

  Public:
    /property/[id]                        Updated: owner contact section
                                          conditionally shows name/phone

─────────────────────────────────────────
  1.10  SHORT RENT COMMISSION (ADMIN SETTING)
─────────────────────────────────────────

  Since short rent properties are free to publish, the platform monetises
  short rent through a fixed commission percentage on every booking payment.

  Admin sets this in: Admin Dashboard → Settings → "Short Rent Commission"

  How it works:
  • Admin enters a commission percentage (e.g., 10%)
  • This value is stored in a global AdminSettings record
  • When a short rent booking payment is completed via Razorpay:
      - Platform keeps:  totalAmount × commissionPercent / 100
      - Owner receives:  totalAmount − platformCommission
  • Both amounts are recorded on the Payment record for transparency
  • Admin can change the % at any time; the new rate applies to future
    bookings only — already-paid bookings are not recalculated

  Admin Settings fields for commission:
  ─────────────────────────────────────────
  shortRentCommissionPercent   Float   (e.g. 10 = 10%; range 0–100)

  Database — NEW model: AdminSettings
  ─────────────────────────────────────────
  model AdminSettings {
    id                          String  @id @default("singleton")
    shortRentCommissionPercent  Float   @default(0)
    // future global settings can be added here
    updatedAt                   DateTime @updatedAt
  }
  (Single row, id always = "singleton". Use upsert to update.)

  Payment model — add commission fields:
  ─────────────────────────────────────────
  commissionPercent   Float?   // rate applied at time of payment
  commissionAmount    Float?   // actual GBP amount taken by platform
  ownerEarnings       Float?   // amount owner receives (amount - commission)

  Admin Settings UI:
    /admin/settings                       Settings page with "Short Rent
                                          Commission" input field (number, %)
                                          Save button. Shows current value.

══════════════════════════════════════════════════════════════════════════════
  PHASE 2 — PROPERTY DOCUMENTS
  "Admin defines required document types; owners upload per property"
══════════════════════════════════════════════════════════════════════════════

  DEPENDS ON: Phase 1 for LONG_RENT / BUY documents.
              SHORT_TERM document types (if any) apply independently.

─────────────────────────────────────────
  2.1  OVERVIEW
─────────────────────────────────────────

  Currently, documents are for USER/OWNER identity verification only.
  Phase 2 adds a separate document layer per PROPERTY.

  Admin creates document type definitions (e.g., "Electricity Bill",
  "Gas Safety Certificate"). Each definition includes:
  • Whether the document is REQUIRED or optional
  • Whether an Issue Date must be provided
  • Whether an Expiry Date must be provided

  When an owner adds or edits a property, they must upload all required
  document types before the property can be published.

─────────────────────────────────────────
  2.2  ADMIN — DOCUMENT TYPE MANAGEMENT
─────────────────────────────────────────

  Where: Admin Dashboard → "Property Document Types"

  Actions:
  • Create document type
  • Edit document type
  • Mark active / inactive (inactive types are no longer required on new listings)
  • Delete (only if no property has uploaded this type)

  Required fields when creating a document type:
  ────────────────────────────────────────────────
  - Document Name          (e.g., "Electricity Bill", "Gas Safety Certificate")
  - Description            (optional helper text shown to owner)
  - Is Required?           (Yes / No — if Yes, owner cannot publish without it)
  - Require Issue Date?    (Yes / No)
  - Require Expiry Date?   (Yes / No)
  - Applies To             (ALL / LONG_RENT / BUY / SHORT_RENT — can be multi-select)

  Examples:
  ─────────────────────────────────────────────────────────────────────────
  Name                    │ Required │ Issue Date │ Expiry Date │ Applies To
  ────────────────────────┼──────────┼────────────┼─────────────┼──────────
  Electricity Bill        │ Yes      │ Yes        │ Yes         │ ALL
  Gas Safety Certificate  │ Yes      │ Yes        │ Yes         │ LONG_RENT
  EPC Certificate         │ Yes      │ Yes        │ Yes         │ BUY, LONG_RENT
  Floor Plan              │ No       │ No         │ No          │ ALL
  Title Deed              │ Yes      │ Yes        │ No          │ BUY
  ─────────────────────────────────────────────────────────────────────────

─────────────────────────────────────────
  2.3  OWNER — UPLOADING PROPERTY DOCUMENTS
─────────────────────────────────────────

  Where: Owner adds/edits a property → "Documents" tab/step

  Flow:
  1. Owner opens the property form
  2. "Documents" section shows all ACTIVE required document types
     filtered to the property's listing type (LONG_RENT / BUY / SHORT_RENT)
  3. For each document type:
     • Upload button (Cloudinary upload)
     • Issue Date field (shown only if Require Issue Date = Yes)
     • Expiry Date field (shown only if Require Expiry Date = Yes)
  4. Owner cannot submit "Publish" until all required documents are uploaded
     and all required dates are filled in
  5. Admin can see uploaded documents on the property detail in admin panel
  6. Admin can verify or reject a document (similar to identity document flow)

─────────────────────────────────────────
  2.4  EXPIRY ALERTS
─────────────────────────────────────────

  A cron job (or Vercel cron) runs daily:

  1. Finds all PropertyDocument records where:
     - expiryDate is within the next 30 days
     - property status = ACTIVE
     - owner's package has docExpiryAlert = true

  2. Creates a Notification for the owner:
     "Your [Document Name] for [Property Title] expires on [date]. 
      Please upload a new document to keep your listing active."

  3. If expiryDate has passed:
     - Property status is set to INACTIVE
     - Owner gets a notification: "Your listing has been paused due to an
       expired [Document Name]. Upload a new document to re-activate."
     - Admin also gets notified

─────────────────────────────────────────
  2.5  DATABASE CHANGES — PHASE 2
─────────────────────────────────────────

  NEW model: PropertyDocumentType
  ─────────────────────────────────
  model PropertyDocumentType {
    id              String   @id @default(cuid())
    name            String   @unique
    description     String?
    isRequired      Boolean  @default(true)
    requireIssueDate  Boolean  @default(false)
    requireExpiryDate Boolean  @default(false)
    appliesTo       String[] @default([])  // ["ALL"] or ["LONG_RENT","BUY"] etc.
    isActive        Boolean  @default(true)
    sortOrder       Int      @default(0)
    createdAt       DateTime @default(now())
    updatedAt       DateTime @updatedAt

    propertyDocuments PropertyDocument[]
  }

  NEW model: PropertyDocument
  ────────────────────────────
  model PropertyDocument {
    id             String   @id @default(cuid())
    documentUrl    String
    fileName       String
    fileSize       Int
    mimeType       String?
    issuedDate     DateTime?
    expiryDate     DateTime?
    status         String   @default("PENDING")  // PENDING, VERIFIED, REJECTED, EXPIRED
    verifiedNotes  String?
    verifiedBy     String?
    verifiedAt     DateTime?

    propertyId         String
    property           Property             @relation(...)
    documentTypeId     String
    documentType       PropertyDocumentType @relation(...)

    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    @@index([propertyId])
    @@index([documentTypeId])
    @@index([expiryDate])
    @@index([status])
  }

  Update Property model: add relation
    propertyDocuments  PropertyDocument[]

  New migration: 20260508_add_property_document_types

─────────────────────────────────────────
  2.6  NEW API ENDPOINTS — PHASE 2
─────────────────────────────────────────

  Admin — Document Type Management:
  GET    /api/admin/property-document-types          List all
  POST   /api/admin/property-document-types          Create
  PATCH  /api/admin/property-document-types/[id]     Edit
  DELETE /api/admin/property-document-types/[id]     Delete

  Owner — Property Documents:
  GET    /api/properties/[id]/documents              Get docs for a property
  POST   /api/properties/[id]/documents              Upload a document
  PATCH  /api/properties/[id]/documents/[docId]      Update (re-upload / update dates)
  DELETE /api/properties/[id]/documents/[docId]      Remove a document

  Admin — Verify Property Documents:
  PATCH  /api/admin/property-documents/[docId]/verify   Verify / reject a doc

  Public:
  GET    /api/properties/[id]/documents/required        Returns list of required
                                                         document types for the
                                                         listing type (for frontend
                                                         form validation)

─────────────────────────────────────────
  2.7  FRONTEND PAGES / COMPONENTS — PHASE 2
─────────────────────────────────────────

  Admin:
    /admin/property-document-types              List + manage document types
    /admin/property-document-types/new          Create form
    /admin/properties/[id]                      Shows uploaded documents tab
                                                with verify/reject action

  Owner Dashboard:
    /owner/dashboard/properties/[id]/edit       Documents step/tab added to
                                                property form
    Property form → Documents section:          Upload widget per document type,
                                                issue/expiry date pickers,
                                                upload status badge

  Notifications:
    Existing notification UI shows expiry alerts with link to upload

══════════════════════════════════════════════════════════════════════════════
  PHASE 3 — BIDDING / BOOST FOR SHORT RENT PROPERTIES
  "Owners bid to appear at the top of search results for a zip code"
══════════════════════════════════════════════════════════════════════════════

  DEPENDS ON: Phase 1 (Admin Settings / commission infrastructure)
  NO PACKAGE REQUIRED — any owner with an active SHORT_TERM listing can bid.

─────────────────────────────────────────
  3.1  OVERVIEW
─────────────────────────────────────────

  This works like a paid boost / ad placement, similar to Google Ads or
  Zoopla featured listings:

  • Only SHORT_TERM (short rent) properties can use bidding
  • ANY owner with an active SHORT_TERM property can place a bid —
    no package purchase required
  • An owner places a bid for a specific zip code
  • The highest active bid for that zip code appears FIRST in search results
    for that zip code
  • Bids have a time limit (owner sets start date + end date, up to 30 days)
  • Payment is taken upfront via Razorpay for the full bid duration
  • If a higher bid comes in, the lower bidder is NOT automatically displaced —
    they retain their slot for the paid duration. The higher bidder goes above.
  • Admin can see all active bids and override if needed

─────────────────────────────────────────
  3.2  HOW BID RANKING WORKS
─────────────────────────────────────────

  When a user searches short rent properties for a zip code:

  1. Query all ACTIVE bids for that zip code where now() is between
     bid.startDate and bid.endDate
  2. Sort those properties by bid.amount DESC (highest bid first)
  3. Place those properties at the top of the results (marked with a
     small "Featured" or "Boosted" badge)
  4. Below the boosted listings, show the regular results sorted by
     the normal algorithm (createdAt, rating, etc.)

  Multiple bids for same zip code — all show boosted, sorted by amount.
  Maximum 3 boosted listings per zip code page at one time (Admin configurable).

─────────────────────────────────────────
  3.3  OWNER — PLACING A BID
─────────────────────────────────────────

  Where: Owner Dashboard → Properties → [Property] → "Boost / Bid"

  Flow:
  1. Owner selects a SHORT_RENT property
  2. Clicks "Boost This Property"
  3. Form shows:
     • Zip Code to target        (pre-filled from property's zipCode, editable)
     • Bid Amount (£/day)        (e.g., £5.00/day)
     • Start Date                (today or future date)
     • End Date                  (max 30 days from start)
     • Total Cost preview        = bid amount × number of days
     • Current highest bid for that zip code (if any) shown for reference
  4. Owner confirms → Razorpay payment for total cost
  5. Payment success → PropertyBid record created with status = ACTIVE
  6. Property now appears boosted in that zip code search

  Validation rules:
  • Minimum bid: £1.00/day (Admin configurable in AdminSettings)
  • Minimum duration: 1 day
  • Maximum duration: 30 days (Admin configurable)
  • Property must be ACTIVE and SHORT_TERM
  • No package required — any owner can bid on their SHORT_TERM property
  • Cannot have two overlapping active bids for the same property + zip code

─────────────────────────────────────────
  3.4  BID EXPIRY
─────────────────────────────────────────

  • When endDate passes, bid status automatically becomes EXPIRED
    (handled by cron job or checked at query time)
  • No refunds for expired bids (already paid for the days)
  • Owner gets a notification 2 days before bid expires:
    "Your boost for [Property] in [Zip Code] expires on [date]. 
     Renew to keep your top placement."

─────────────────────────────────────────
  3.5  ADMIN — BID MANAGEMENT
─────────────────────────────────────────

  Where: Admin Dashboard → "Bids / Boosts"

  Admin can:
  • View all active, expired, and cancelled bids
  • See bid amount, zip code, property, owner, dates, revenue
  • Cancel a bid (with refund trigger via Razorpay)
  • Configure in Admin Settings:
      - Minimum bid amount per day (£)
      - Maximum bid duration (days)
      - Max boosted slots visible per zip code page

─────────────────────────────────────────
  3.6  DATABASE CHANGES — PHASE 3
─────────────────────────────────────────

  NEW model: PropertyBid
  ───────────────────────
  model PropertyBid {
    id          String   @id @default(cuid())
    zipCode     String                         // target zip code
    amount      Float                          // £ per day
    totalCost   Float                          // amount × days
    startDate   DateTime
    endDate     DateTime
    status      String   @default("ACTIVE")   // ACTIVE, EXPIRED, CANCELLED

    // Payment
    paymentId   String?
    razorpayOrderId   String?
    razorpayPaymentId String?

    // Relations
    propertyId  String
    property    Property @relation(...)
    ownerId     String
    owner       User     @relation(...)

    createdAt   DateTime @default(now())
    updatedAt   DateTime @updatedAt

    @@index([zipCode])
    @@index([status])
    @@index([endDate])
    @@index([propertyId])
    @@index([ownerId])
  }

  Update Property model: add relation
    bids  PropertyBid[]

  New migration: 20260508_add_property_bids

─────────────────────────────────────────
  3.7  NEW API ENDPOINTS — PHASE 3
─────────────────────────────────────────

  Owner:
  GET    /api/owner/bids                         Owner's bid history
  POST   /api/owner/bids                         Place a new bid (+ Razorpay order)
  POST   /api/owner/bids/[id]/payment/verify     Verify Razorpay payment
  DELETE /api/owner/bids/[id]                    Cancel a bid (within 1 hour grace)

  Admin:
  GET    /api/admin/bids                         All bids (filterable)
  PATCH  /api/admin/bids/[id]/cancel             Cancel + trigger refund
  GET    /api/admin/settings                     Get all admin settings (incl. bids)
  PATCH  /api/admin/settings                     Update settings (min bid, max slots,
                                                 commission %, etc.)

  Public (used by search):
  GET    /api/properties?zipCode=SW1A&...        Updated to include boosted sort

─────────────────────────────────────────
  3.8  FRONTEND PAGES / COMPONENTS — PHASE 3
─────────────────────────────────────────

  Owner Dashboard:
    /owner/dashboard/properties/[id]/boost       Bid placement form
    /owner/dashboard/bids                        Owner's bid history, active bids

  Admin:
    /admin/bids                                  All bids list + stats
    /admin/bids/settings                         Configure bid rules

  Public:
    Short rent search results page               Boosted properties appear first
                                                 with "Boosted" badge
    Property card                                Small "Boosted" label if active bid

══════════════════════════════════════════════════════════════════════════════
  PHASE 4 — CHAT-BASED INQUIRIES WITH REMINDERS
  "Inquiries become persistent chat threads — like SpareRoom"
══════════════════════════════════════════════════════════════════════════════

  DEPENDS ON: Phase 1 (package determines who receives inquiry messages)
  CAN START in parallel with Phase 3 if separate developer available.

─────────────────────────────────────────
  4.1  OVERVIEW
─────────────────────────────────────────

  Currently, inquiries are a single message with a status. Phase 4 converts
  them into persistent threaded conversations modelled on SpareRoom's messaging
  UX (see attached screenshots).

  KEY BEHAVIOUR CHANGE (CRITICAL — fix immediately in current code):
  ──────────────────────────────────────────────────────────────────
  Currently the "Message" button / inquiry form on the property detail page
  allows unauthenticated users to send a message. THIS MUST CHANGE.

  New rule: A user MUST be logged in to send an inquiry.
    • If NOT logged in and user clicks "Message" on a property:
        → Show a prompt inside the message panel:
          "You need an account to send a message."
          [Log in]  [Sign up]
          Both buttons redirect to /login or /signup with a returnUrl back to
          the property page so the user lands back after authenticating.
    • If logged in → show the message compose form as described in 4.2.

  After the login/signup gate is in place, the full chat system works:
  • User sends first message → Inquiry thread created
  • Owner replies → message appended to same thread
  • Both parties can send unlimited messages back and forth
  • All history is visible in chronological order in the chat window
  • Owner and Admin can set viewing/reminder events on the thread

─────────────────────────────────────────
  4.2  PROPERTY PAGE — MESSAGE BUTTON & COMPOSE FORM
─────────────────────────────────────────

  On the public property detail page (/property/[id]):

  The page has two tabs: [Ad details] | [Message]
  (Ref: screenshots — same tab layout as SpareRoom)

  Tab behaviour:
  ──────────────
  "Ad details" tab → shows property info (current behaviour, unchanged)
  "Message" tab    → shows compose form or login prompt depending on auth state

  NOT logged in — "Message" tab shows:
  ┌──────────────────────────────────────────────────┐
  │  Message Lettings                                │
  │                                                  │
  │  You need an account to message this listing.    │
  │                                                  │
  │  [  Log in  ]   [  Sign up  ]                    │
  └──────────────────────────────────────────────────┘
  After login/signup, user is redirected back to /property/[id]#message

  Logged in — "Message" tab shows compose form:
  ┌──────────────────────────────────────────────────┐
  │  Message Lettings                                │
  │  [owner avatar / logo]                           │
  │  RE: [Property Title]                            │
  │                                                  │
  │  Enter your message                              │
  │  ┌────────────────────────────────────────────┐  │
  │  │                                            │  │
  │  │  Hi Lettings…  (placeholder)               │  │
  │  │                                            │  │
  │  └────────────────────────────────────────────┘  │
  │                                                  │
  │  From: [User full name]  (Not you? Log in as     │
  │                           other user)            │
  │                   [📧 Send message]               │
  └──────────────────────────────────────────────────┘

  After clicking "Send message":
    • If this is the FIRST message to this property → create Inquiry thread
    • If a thread already exists between this user and this property →
      append message to existing thread (no duplicate threads)
    • Show success banner:
      ✅ "Thank you - your message has been sent. Please be patient
          waiting for a response."
    • User can now go to /dashboard/inquiries to continue the conversation

  If the owner package has directInquiryToOwner = true:
    → Inquiry notification goes directly to owner
  If adminCCOnInquiry = true:
    → Admin also receives notification

─────────────────────────────────────────
  4.3  USER DASHBOARD — MESSAGES PAGE
─────────────────────────────────────────

  Route: /dashboard/inquiries  (also /user/dashboard/inquiries)
  Ref: Screenshots — "My messages" page

  Page layout:
  ┌──────────────────────────────────────────────────────────────────┐
  │  My messages                                                     │
  │                                                                  │
  │  [Inbox (n)] [Sent] [Unread] [Not Replied To] [Deleted]         │
  ├──────────────────────────────────────────────────────────────────┤
  │  Filter by label:  [Show all ▼]        [🏷 Manage labels]       │
  │  Filter by keyword/name: [____________]                          │
  │                           [ Apply filters ]                      │
  ├──────────────────────────────────────────────────────────────────┤
  │  Displaying N conversation(s)                                    │
  │  Sort by: Newest first | Oldest first | By name                  │
  │                                                                  │
  │  ┌──────────────────────────────────────────────────────────┐   │
  │  │ [avatar] Owner/Agent Name (N)              Today/date    │   │
  │  │          PROPERTY TITLE IN CAPS                          │   │
  │  │          last message preview...                         │   │
  │  └──────────────────────────────────────────────────────────┘   │
  │                                                                  │
  │  [☐ Select all]  [🗑 Delete]  [↩ Bulk reply]  [🏷 Add label]   │
  └──────────────────────────────────────────────────────────────────┘

  Tabs behaviour:
  ───────────────
  Inbox          → All threads where user received at least one message
  Sent           → Threads started by this user
  Unread         → Threads with messages not yet read by this user
  Not Replied To → Threads where last message is from owner/admin (user hasn't
                   replied yet)
  Deleted        → Soft-deleted threads (can be restored)

  Filter by label: (see labels list in 4.4 — pre-defined set, user assigns)
  Filter by keyword/name: searches property title and owner name

  Conversation list item shows:
  • Owner/agent avatar (or placeholder house icon)
  • Owner/agent name + unread count in (brackets)
  • Property title (UPPERCASE, matches SpareRoom style)
  • Last message text preview (truncated)
  • Date / "Today" timestamp
  • Reply icon if user has replied (↩)

  Clicking a conversation → opens the Chat Window (see 4.5)

─────────────────────────────────────────
  4.4  LABELS
─────────────────────────────────────────

  Pre-defined labels (exactly as per screenshot — no custom labels in Phase 4):
  ────────────────────────────────────────
    Viewing arranged
    Viewing completed
    Suitable
    Maybe
    Rejected
    Waiting for paperwork
    No Label  (default — removes any label)

  A user can assign ONE label to any inquiry thread.
  Label is stored on the Inquiry record as a String field.
  Labels are only visible to the person who set them (user or owner each
  see their own labels; they are NOT shared between parties).

  Manage labels page: /dashboard/inquiries/labels
    → Shows all pre-defined labels; user can see how many threads have each label.
    → No add/delete labels in Phase 4 (labels are fixed set).

─────────────────────────────────────────
  4.5  CHAT WINDOW (THREAD VIEW)
─────────────────────────────────────────

  Route: /dashboard/inquiries/[id]   (user)
         /owner/dashboard/inquiries/[id]  (owner)
         /admin/inquiries/[id]  (admin)

  Layout:
  ┌────────────────────────────────────────────────────────────┐
  │  ← Go back to messages list                               │
  │  [Property Title]                                          │
  │  [Ad details] [Message]  tabs  —  Ad ref no. XXXXXXXX     │
  ├────────────────────────────────────────────────────────────┤
  │  CHAT MESSAGES AREA (scrollable)                           │
  │                                                            │
  │  [owner avatar] Owner name — [timestamp]                   │
  │  ┌──────────────────────────────────────────────────────┐  │
  │  │  Hi, thanks for your interest in…                   │  │
  │  └──────────────────────────────────────────────────────┘  │
  │                                                            │
  │                    [timestamp]        [user avatar]        │
  │  ┌──────────────────────────────────────────────────────┐  │
  │  │  Hi, I'm interested in viewing the property…        │  │
  │  └──────────────────────────────────────────────────────┘  │
  │                                                            │
  │  [System message — e.g., 📅 Reminder: Viewing 10 May 2pm] │
  ├────────────────────────────────────────────────────────────┤
  │  ACTIONS BAR (below chat):                                 │
  │  [Add note]   [Add label]   [View owner details]           │
  │  [View ad → links to /property/[id]]                       │
  ├────────────────────────────────────────────────────────────┤
  │  REPLY BOX                                                 │
  │  ┌────────────────────────────────────────────────────┐    │
  │  │  Write a reply…                                    │    │
  │  └────────────────────────────────────────────────────┘    │
  │  [Send reply]                                              │
  └────────────────────────────────────────────────────────────┘

  Message bubbles:
  • Owner messages → aligned left, light background
  • User messages  → aligned right, tinted background
  • System messages (STATUS_CHANGE, REMINDER_SET) → centred, italic, gray

  Polling: frontend polls GET /api/inquiries/[id]/messages every 10 seconds
           when the chat window is open (see 4.10 for real-time note).

  "Add note" button:
    → Opens a modal (ref: screenshot — "Add notes" modal):
      ┌──────────────────────────────────────────┐
      │  Add notes                        ✕ Close │
      │  All notes are saved automatically.       │
      │  Only you can see them.                   │
      │  ┌──────────────────────────────────────┐ │
      │  │                                      │ │
      │  │  (resizable textarea)                │ │
      │  │                                      │ │
      │  └──────────────────────────────────────┘ │
      └──────────────────────────────────────────┘
      Notes are private — stored as InquiryNote (one per user per thread,
      auto-saved on blur or typing pause). NOT sent to owner.

  "Add label" button:
    → Opens a dropdown (same label list as 4.4)
    → Applies label to this thread for this user only

  "View owner details" button:
    → Opens a slide-over / modal (ref: screenshot — owner profile panel):
      Shows: owner avatar, name, role (Agent / Owner), "Upgraded" badge if
      package has directInquiryToOwner=true or fullAdminSupport=true,
      Member since [year], Last active [time ago],
      "Ads from [Owner]" — list of their other active properties with link

  "View ad" link:
    → Redirects to /property/[id] (the property detail page)

─────────────────────────────────────────
  4.6  OWNER DASHBOARD — MESSAGES
─────────────────────────────────────────

  Route: /owner/dashboard/inquiries

  Same tab/filter layout as user messages page (4.3), but:
  • Inbox shows messages received FROM users about owner's properties
  • Owner can reply, creating messages in the same thread
  • Owner can set labels and notes for their own reference
  • Owner sees "View property" link (to their property edit page)
  • Owner has an additional action: Set Reminder (see 4.8)
  • Owner can mark a conversation status: Open / Replied / Closed
    (shown as status badge on thread list)

  Thread list for owner shows:
  • User's name + unread count
  • Property title they enquired about
  • Last message preview + timestamp
  • Status badge (Open / Replied / Closed)

─────────────────────────────────────────
  4.7  ADMIN DASHBOARD — MESSAGES
─────────────────────────────────────────

  Route: /admin/inquiries

  Admin sees ALL inquiry threads across ALL properties.
  Filter by: property, owner, user, status, date range.

  Admin can:
  • Read any thread
  • Reply in any thread (appears as "Admin" message bubble)
  • Set reminders (see 4.8)
  • Change inquiry status
  • For owners with adminCCOnInquiry=true: Admin is copied automatically
    on each new message notification

─────────────────────────────────────────
  4.8  CHAT MESSAGE TYPES
─────────────────────────────────────────

  messageType values stored on InquiryMessage:
  ─────────────────────────────────────────────────────────────────────
  TEXT              │ Normal chat message (user ↔ owner ↔ admin)
  STATUS_CHANGE     │ System: "Owner marked inquiry as Replied"
  REMINDER_SET      │ System: "📅 Viewing arranged for 10 May at 2pm"
  ─────────────────────────────────────────────────────────────────────

  Each message records:
  • senderId (userId or null for system messages)
  • senderRole: USER / OWNER / ADMIN / SYSTEM
  • content (text)
  • messageType
  • readBy (array of userIds who have read it)
  • createdAt timestamp

─────────────────────────────────────────
  4.9  REMINDERS (OWNER & ADMIN ONLY)
─────────────────────────────────────────

  Owner or Admin can set a reminder inside any inquiry thread.
  Users cannot set reminders — only owner and admin.

  "Set Reminder" button visible in chat window for owner/admin only.

  Reminder form fields:
  • Title           (e.g., "Property Viewing", "Follow-up Call")
  • Date            (date picker)
  • Time            (time picker)
  • Note            (optional — extra context, private)
  • Remind          (Owner / Admin / Both)

  What happens when reminder is saved:
  1. A system InquiryMessage is added to the thread (visible to all):
     "📅 Reminder set: [Title] on [Date] at [Time]"
     (This appears in SpareRoom style as a "Viewing arranged" system bubble)
  2. An InquiryReminder record is saved
  3. Notification sent to the specified party:
     - 24 hours before scheduledAt: "Tomorrow: [Title] — [Property Title]"
     - 1 hour before scheduledAt: "In 1 hour: [Title] — [Property Title]"
  4. Notification links directly to /owner/dashboard/inquiries/[id]

  Label auto-update: when a reminder with title containing "Viewing" is set,
  optionally auto-apply label "Viewing arranged" to the thread.

─────────────────────────────────────────
  4.10  PRIVATE NOTES (ALL ROLES)
─────────────────────────────────────────

  Every party (user, owner, admin) can add a private note to a thread.
  Notes are:
  • Stored in InquiryNote model (one note per user per thread, upserted)
  • Visible ONLY to the person who wrote them
  • Auto-saved on textarea blur or with a debounced save (500ms)
  • Accessible via the "Add note" button in the chat window (modal, as per screenshot)
  • Do NOT appear in the message thread itself
  • Do NOT get sent to the other party

  InquiryNote model (already exists — keep unchanged, just wire to chat UI):
  • inquiryId, userId, content, updatedAt

─────────────────────────────────────────
  4.11  REAL-TIME vs POLLING
─────────────────────────────────────────

  Phase 4: Simple polling (10-second interval when chat window is open)
  • GET /api/inquiries/[id]/messages?after=[lastMessageId]
  • Returns only messages newer than lastMessageId → merge into local state
  • Works on Vercel serverless, no extra infrastructure

  Phase 4+: Upgrade to Server-Sent Events or WebSockets
  • Requires Vercel Edge or a standalone WebSocket server
  • Recommended as future upgrade if polling feels slow

─────────────────────────────────────────
  4.12  DATABASE CHANGES — PHASE 4
─────────────────────────────────────────

  Existing Inquiry model — add fields:
  ──────────────────────────────────────────────────────────────────
  lastMessageAt   DateTime?    // for sorting threads by most recent activity
  lastMessageBy   String?      // userId of last sender
  unreadByOwner   Int   @default(0)
  unreadByUser    Int   @default(0)
  unreadByAdmin   Int   @default(0)
  label           String?      // user-assigned label (per-party, see 4.4)
                               // NOTE: label is private per viewer — store
                               // as InquiryLabel join model if multi-party
                               // labels needed, otherwise one field per role
  userLabel       String?      // label set by the user
  ownerLabel      String?      // label set by the owner
  status          String  @default("OPEN")  // OPEN, REPLIED, CLOSED
  isDeletedByUser Boolean @default(false)   // soft delete for user's view
  isDeletedByOwner Boolean @default(false)  // soft delete for owner's view

  NEW model: InquiryMessage
  ──────────────────────────
  model InquiryMessage {
    id          String   @id @default(cuid())
    content     String
    messageType String   @default("TEXT")    // TEXT, STATUS_CHANGE, REMINDER_SET
    senderRole  String                       // USER, OWNER, ADMIN, SYSTEM

    inquiryId   String
    inquiry     Inquiry  @relation(fields: [inquiryId], references: [id])
    senderId    String?  // null for SYSTEM messages
    sender      User?    @relation(fields: [senderId], references: [id])

    readBy      String[] @default([])        // array of userIds who have read

    createdAt   DateTime @default(now())

    @@index([inquiryId])
    @@index([createdAt])
    @@index([senderId])
  }

  NEW model: InquiryReminder
  ───────────────────────────
  model InquiryReminder {
    id          String   @id @default(cuid())
    title       String
    scheduledAt DateTime
    note        String?
    remindOwner Boolean  @default(true)
    remindAdmin Boolean  @default(false)
    isCompleted Boolean  @default(false)
    notifiedAt  DateTime?

    inquiryId   String
    inquiry     Inquiry  @relation(fields: [inquiryId], references: [id])
    createdById String
    createdBy   User     @relation(fields: [createdById], references: [id])

    createdAt   DateTime @default(now())
    updatedAt   DateTime @updatedAt

    @@index([inquiryId])
    @@index([scheduledAt])
  }

  Existing InquiryNote model — KEEP UNCHANGED, just connect to chat UI.

  New migration: 20260510_add_inquiry_chat

─────────────────────────────────────────
  4.13  NEW / UPDATED API ENDPOINTS — PHASE 4
─────────────────────────────────────────

  Public property page (auth gate — CHANGE EXISTING):
  POST   /api/inquiries                           Creates thread + first message
                                                  NOW REQUIRES auth (401 if not
                                                  logged in — frontend shows
                                                  login/signup prompt instead)

  User / Owner / Admin (gated by role):
  GET    /api/inquiries                           Thread list for logged-in user
                                                  Query params: tab (inbox/sent/
                                                  unread/not-replied/deleted),
                                                  label, keyword, sort
  GET    /api/inquiries/[id]                      Thread header + last 50 messages
  GET    /api/inquiries/[id]/messages             Paginated messages
                                                  Query: after=[msgId] for polling
  POST   /api/inquiries/[id]/messages             Send a reply in the thread
  PATCH  /api/inquiries/[id]/read                 Mark all messages as read
  PATCH  /api/inquiries/[id]/label                Set label on thread (per-user)
  PATCH  /api/inquiries/[id]/status               Change status (owner/admin only)
  DELETE /api/inquiries/[id]                      Soft-delete thread for this user

  Notes (existing — wire to chat UI):
  GET    /api/inquiries/[id]/note                 Get private note for this user
  PUT    /api/inquiries/[id]/note                 Upsert private note (auto-save)

  Reminders (owner + admin only):
  POST   /api/inquiries/[id]/reminders            Create reminder
  GET    /api/inquiries/[id]/reminders            List reminders for this thread
  PATCH  /api/inquiries/[id]/reminders/[rid]      Mark reminder complete / edit

  Admin only:
  GET    /api/admin/inquiries                     All threads (all properties),
                                                  filterable by property/owner/
                                                  user/status/date
  PATCH  /api/admin/inquiries/[id]                Force-change status or assign

─────────────────────────────────────────
  4.14  FRONTEND PAGES & COMPONENTS — PHASE 4
─────────────────────────────────────────

  Property Detail Page (change existing):
    /property/[id]                        "Message" tab
    Component: InquiryComposeForm         Shows login/signup gate OR compose form
                                          Displays: property ref, owner logo,
                                          "RE: [title]", From: [user name],
                                          Send button, success banner after send

  User Dashboard (new pages):
    /dashboard/inquiries                  "My messages" page
    Component: MessageTabBar              Tabs: Inbox / Sent / Unread /
                                          Not Replied To / Deleted
    Component: MessageFilters             Label filter dropdown, keyword search,
                                          Apply filters, sort buttons
    Component: ConversationListItem       Avatar, owner name, property title CAPS,
                                          last message preview, date, unread badge
    /dashboard/inquiries/[id]            Chat window
    Component: ChatWindow                 Message bubbles (left/right), system msgs
    Component: ChatReplyBox               Textarea + Send reply button
    Component: AddNoteModal               Private note modal (auto-save)
    Component: LabelDropdown             Pre-defined labels picker
    Component: OwnerProfilePanel          Slide-over: avatar, name, role, member
                                          since, last active, other ads list
    Actions in chat: Add note | Add label | View owner details | View ad (→ /property/[id])

  Owner Dashboard (update existing + new):
    /owner/dashboard/inquiries            Inbox page (same layout as user)
    Component: OwnerConversationItem      Shows user name, property title, status
                                          badge (Open/Replied/Closed), label
    /owner/dashboard/inquiries/[id]       Chat window (same as user but with extra
                                          actions: Set Reminder, Change Status)
    Component: ReminderModal              Title, date, time, note, remind-who picker

  Admin Dashboard (update existing):
    /admin/inquiries                      All threads with advanced filters
    /admin/inquiries/[id]                 Full chat window + admin reply + reminder +
                                          status change + owner/user profile links

══════════════════════════════════════════════════════════════════════════════
  DEPENDENCY MAP (VISUAL SUMMARY)
══════════════════════════════════════════════════════════════════════════════

  ┌─────────────────────────────────────────────────────────────────────┐
  │  PHASE 1: Packages                                                  │
  │  ─ Package model feature flags                                      │
  │  ─ Owner purchase flow (Razorpay)                                   │
  │  ─ Property publish gate                                            │
  │  ─ Owner contact visibility on listing                              │
  └──────────────────┬──────────────────────────────────────────────────┘
                     │
           ┌─────────┴──────────┐
           │                    │
           ▼                    ▼
  ┌─────────────────┐  ┌─────────────────────────────────────────────┐
  │ PHASE 2:        │  │ PHASE 3: Bidding                            │
  │ Property Docs   │  │ ─ PropertyBid model                         │
  │ ─ DocType model │  │ ─ Bid placement + Razorpay                  │
  │ ─ PropDoc model │  │ ─ Boosted sort in search                    │
  │ ─ Expiry alerts │  │ ─ Expiry cron + notifications               │
  └────────┬────────┘  └─────────────────────────────────────────────┘
           │
           │  (Phase 4 can start in parallel with Phase 3)
           ▼
  ┌─────────────────────────────────────────────────────────────────────┐
  │  PHASE 4: Chat Inquiries                                            │
  │  ─ InquiryMessage model                                             │
  │  ─ InquiryReminder model                                            │
  │  ─ Chat UI (thread list + chat window)                              │
  │  ─ Reminder notifications                                           │
  └─────────────────────────────────────────────────────────────────────┘

══════════════════════════════════════════════════════════════════════════════
  DATABASE MIGRATIONS SUMMARY
══════════════════════════════════════════════════════════════════════════════

  Phase 1:  20260508_add_package_feature_flags
            ALTER Package — add 7 boolean feature flag columns

  Phase 2:  20260508_add_property_document_types
            CREATE PropertyDocumentType
            CREATE PropertyDocument
            ALTER Property — add propertyDocuments relation

  Phase 3:  20260508_add_property_bids
            CREATE PropertyBid
            ALTER Property — add bids relation

  Phase 4:  20260510_add_inquiry_chat
            CREATE InquiryMessage
            CREATE InquiryReminder
            ALTER Inquiry — add lastMessageAt, lastMessageBy,
                            unreadByOwner, unreadByUser, unreadByAdmin,
                            userLabel, ownerLabel, status,
                            isDeletedByUser, isDeletedByOwner
            KEEP InquiryNote model unchanged (wired to chat UI, not migrated)

══════════════════════════════════════════════════════════════════════════════
  ESTIMATED EFFORT (ROUGH GUIDE)
══════════════════════════════════════════════════════════════════════════════

  Phase 1 — Packages          ~5–7 days  (DB + Admin CRUD + Purchase flow + Gate)
  Phase 2 — Property Docs     ~3–5 days  (DB + Admin CRUD + Owner upload + Alerts)
  Phase 3 — Bidding           ~4–6 days  (DB + Bid form + Razorpay + Search sort)
  Phase 4 — Chat Inquiries    ~7–10 days (DB + auth gate fix + thread list +
                                           chat window + labels + notes UI +
                                           owner profile panel + reminders +
                                           polling + admin view)

  Total (sequential):  ~18–27 days
  Total (Phase 3 & 4 parallel after Phase 2): ~14–20 days

══════════════════════════════════════════════════════════════════════════════
  END OF DOCUMENT
══════════════════════════════════════════════════════════════════════════════
